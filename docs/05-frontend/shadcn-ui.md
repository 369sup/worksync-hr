# shadcn/ui

## 目的
- 定義 UI 元件重用策略。

## 圖解
- 優先層次：shadcn/ui 元件 > 專案共用元件 > 頁面專用組件。

## 規則
- 優先重用既有元件與 variant。
- 不為單一頁面需求複製大量近似元件。

## 範例
- 表單優先使用既有 input、dialog、table 模式。

## 維護注意事項
- 元件變體增加時同步整理命名規則。
